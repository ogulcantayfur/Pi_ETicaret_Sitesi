@model List<Kategori>

<div class="list-group">
    @foreach (var kategori in Model)
    {
        if(kategori.ad=="Matematik")
        {
            break;
        }
        <a href="#" class="list-group-item list-group-item-action">
            @kategori.ad
        </a>

    }

</div>